name: Setup
description: Runs basic commands to install dependencies
runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 18
    - name: npm install
      run: npm install
      shell: bash
